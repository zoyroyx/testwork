# Database connection string (PostgreSQL for production, sqlite+aiosqlite for local development)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/approval_db
# ENVIRONMENT can be: development, production, testing
ENVIRONMENT=development
